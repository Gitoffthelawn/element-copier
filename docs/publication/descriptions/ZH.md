ELEMENT COPIER
以多种格式复制和下载网页的任意部分。适用于日常工作和专业任务。

功能
- 复制整页或某个元素，
- 一次将信息转换为多种格式，
- 为所有格式保留最近一次复制的内容，
- 浅色和深色主题，
- 灵活的设置。

复制格式
用于处理内容：可直接粘贴到文档中的文本、PNG 或 JPEG 图像，以及用于笔记和文档的标记文本（Markdown）。
用于开发、测试和缺陷报告：页面和所选元素的代码、其样式，以及准确定位元素的方式：CSS 选择器、JS 路径、XPath、完整 XPath 或 tag#id.class。

安全性
- 不跟踪用户，
- 不收集也不传输数据。
